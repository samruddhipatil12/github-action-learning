name: Learn basic of GitHub Action

on:
  push:
    branches:
      -  master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: print chalo
        run: echo "chalo world !!"

      - name: print chalo
        run: echo "chalo world !!"
