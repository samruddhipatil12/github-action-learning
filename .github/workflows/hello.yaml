name: Learn basic of GitHub Action

on:
  push:
    branches:
      -  main

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest

    steps:
      - name: print chalo
        run: cat /etc/os-release

      
