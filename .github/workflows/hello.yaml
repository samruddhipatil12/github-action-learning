name: Learn basic of GitHub Action

on:
  push:
    branches:
      -  main

jobs:
  helloworld:
    runs-on: ubuntu-latest

    steps:
      - name: print chalo
        run: echo "chalo world !!"

      - name: print hello
        run: echo "hello world !!"
